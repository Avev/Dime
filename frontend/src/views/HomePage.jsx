import React from 'react';
import styled from 'styled-components';

const HomePage = ({}) => {
  return <Container>Hello from homepage</Container>;
};

const Container = styled.div``;

export default HomePage;
